import Typography from '@/components/atoms/Typography';

const HomePage = () => {
  return (
    <div className="items-center justify-items-center">
      <Typography is="h1">NEXT JS</Typography>
    </div>
  );
};

export default HomePage;
